/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NetworkDirectiveDirective } from './network-directive.directive';

describe('NetworkDirectiveDirective', () => {
  it('should create an instance', () => {
    let directive = new NetworkDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
